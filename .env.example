DATABASE_URL=postgresql://...

NEXTAUTH_SECRET=your-secret-here
NEXTAUTH_URL=http://localhost:3000

# Required — signs the staff login session cookie (custom JWT via `jose`, not NextAuth).
# Must be at least 32 characters. Generate one with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=

# Email notifications (Nodemailer + Gmail App Password)
# Step 1: Go to https://myaccount.google.com/apppasswords
# Step 2: Create an App Password (select "Mail" + "Windows Computer" or any)
# Step 3: Copy the 16-character password (walang spaces) and paste below
GMAIL_USER=engrpaulojimena@gmail.com
GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx

RESORT_NAME="Your Resort"
RESORT_LOGO_URL=
